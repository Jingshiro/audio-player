FROM node:20-alpine

WORKDIR /app

# Copy package files and install dependencies
COPY backend/package*.json ./
RUN npm ci --omit=dev

# Copy backend source
COPY backend/ ./

# Create data directories
RUN mkdir -p /app/data/audio /app/data/lyrics

EXPOSE 3000

CMD ["node", "src/index.js"]
